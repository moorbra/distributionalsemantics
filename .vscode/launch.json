{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach (Remote Debug)",
            "type": "python",
            "request": "attach",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/semantics/",
            "port": 8000,
            //"secret": "my_secret",
            "host": "localhost",
            "restart":"true"
        }
    ]
}